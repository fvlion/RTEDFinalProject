services:
  app:
    container_name: application
    build:
      context: .
      dockerfile: Dockerfile.local
    image: application
    volumes:
      - ./:/var/www/html
    ports:
      - 8080:80
      - 433:433
